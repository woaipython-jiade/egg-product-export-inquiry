# /design-system — Visual Design Reference

Design tokens, component specs, and style rules for the platform.

| File | Description |
|------|-------------|
| `MASTER.md` | Global design system — colors, typography, spacing, component rules, interaction patterns |
| `pages/` | Page-specific design overrides (when a page deviates from master) |
